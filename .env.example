# copy this file to .env and fill in real values. Never commit .env.
PORT=3000

# optional: messages are always saved to data/messages.jsonl,
# but with SMTP configured they are also emailed to CONTACT_TO
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
CONTACT_TO=yogeshpant911@gmail.com
